# Rename to okf-ci.yml only after bootstrap review.
name: OKF validation (disabled)
on:
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Replace with pinned repository validation commands"
