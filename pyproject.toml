[project]
name = "okf-uk-living"
version = "0.0.0"
description = "Authoring and validation tooling for A Life in the UK"
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0,<7",
]

[tool.uv]
package = false
